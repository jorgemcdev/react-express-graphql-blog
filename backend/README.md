# React Graphql Express Apollo

## Docker Mongo

docker exec -it "name" bash
mongo --port 27017 -u "root" -p "example" --authenticationDatabase "admin"

# Docker Volumes

docker volumes ls
docker volumes rm [name]
